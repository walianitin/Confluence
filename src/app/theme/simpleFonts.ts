/**
 * Simple local font config
 * 
 * 1. Drop your .ttf/.otf files into /local.fonts/
 * 2. Update the filenames below
 * 3. Refresh the page — that's it!
 */

export const LOCAL_FONTS = {
  // File in /local.fonts/ folder (just the filename)
  navbar: "Gothvetica.ttf",
  heading: "InterTight-Regular.ttf", 
  body: "InterTight-Regular.ttf",
  
  // Add more if needed:
  // button: "SomeOtherFont.ttf",
  // caption: "AnotherFont.otf",
};

// Optional: Fine-tune font weights if your files support them
export const FONT_WEIGHTS = {
  navbar: 400,
  heading: 600,
  body: 400,
};
